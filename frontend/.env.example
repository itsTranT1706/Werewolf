# API Gateway URL - the single entry point for all backend communication
VITE_API_GATEWAY_URL=http://localhost:80

# Socket URL for API Gateway (chat, game events)
VITE_SOCKET_URL=http://localhost:8080

# Room Socket URL for Room Service
VITE_ROOM_SOCKET_URL=http://localhost:8082

# Optional: API timeout in milliseconds
VITE_API_TIMEOUT=30000
